var React = require(`react`);

var SectionDetails = React.createClass({

  render: function() {
    return (
      <div>
        Nothing interesting here, but it's a nested route so: wahey.
      </div>
    );
  }

});

module.exports = SectionDetails;
